{
  "name": "@stepflow/samples",
  "version": "0.1.0",
  "description": "Generic reference jobs demonstrating stepflow.",
  "private": true,
  "type": "module",
  "license": "MIT",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@stepflow/core": "^0.1.0"
  },
  "devDependencies": {
    "@stepflow/test": "^0.1.0",
    "puppeteer": "^24.0.0"
  }
}
