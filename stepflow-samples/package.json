{
  "name": "@stepflow/samples",
  "version": "0.2.0",
  "description": "Generic reference jobs demonstrating stepflow.",
  "private": true,
  "type": "module",
  "license": "MIT",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "@stepflow/core": "^0.2.0"
  },
  "devDependencies": {
    "@stepflow/test": "^0.2.0",
    "puppeteer": "^24.0.0"
  }
}
